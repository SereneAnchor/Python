# Agent Instructions for Python Projects

## 目标

本项目可能较大，请优先节省 token，避免无关文件读取。

你需要遵循“先定位，再读取；先计划，再修改”的原则。不要一上来读取整个项目，也不要默认扫描所有源码。

---

## 默认行为

- 允许先查看项目根目录结构和文件名列表，但不要打开源码内容。
- 不要默认读取整个项目。
- 不要默认读取虚拟环境、缓存、构建产物、日志、数据集、模型文件、图片、音频、视频、字体、二进制文件。
- 不要默认读取第三方库源码，除非我明确要求。
- 不要读取或输出敏感信息，例如 `.env`、密钥、token、账号密码、证书、私钥等，除非我明确要求且确有必要。
- 修改文件前必须先给出修改计划。
- 如果需要读取我没有指定的文件，先列出文件名和读取理由，等我确认后再读取。
- 回答尽量简洁，除非我要求详细解释。
- 不要凭空假设文件、目录、类名、函数名存在。所有判断必须来自目录结构、搜索结果、报错信息或我提供的内容。

---

## 项目分析流程

当我要求“分析项目”时，请按以下步骤执行：

1. 先查看目录结构，不打开源码文件。
2. 识别项目类型和运行方式，例如普通脚本、Python 包、Web 后端、爬虫、数据分析、机器学习、自动化脚本、桌面程序等。
3. 识别依赖和构建配置文件，例如：
   - `pyproject.toml`
   - `requirements.txt`
   - `setup.py`
   - `setup.cfg`
   - `Pipfile`
   - `poetry.lock`
   - `uv.lock`
   - `environment.yml`
   - `tox.ini`
   - `noxfile.py`
4. 识别入口文件或入口配置，例如：
   - `main.py`
   - `app.py`
   - `run.py`
   - `manage.py`
   - `__main__.py`
   - `wsgi.py`
   - `asgi.py`
   - `console_scripts`
   - `if __name__ == "__main__"`
5. 识别核心目录，例如：
   - `src/`
   - 项目同名包目录
   - `app/`
   - `core/`
   - `api/`
   - `services/`
   - `models/`
   - `utils/`
   - `tests/`
6. 列出下一步建议读取的 3-5 个关键文件，并说明每个文件为什么重要。
7. 等我确认后，再读取这些文件。
8. 不要一次性读取整个项目。

---

## Python 项目优先级

优先读取：

- `README.md`
- `pyproject.toml`
- `requirements.txt`
- `setup.py`
- `setup.cfg`
- `Pipfile`
- `poetry.lock`
- `uv.lock`
- `environment.yml`
- `pytest.ini`
- `tox.ini`
- `noxfile.py`
- `main.py`
- `app.py`
- `run.py`
- `manage.py`
- `src/**/__init__.py`
- 与问题直接相关的 `.py` 文件
- 与报错直接相关的测试文件

读取顺序建议：

1. 项目说明文件
2. 依赖 / 构建 / 工具配置文件
3. 程序入口
4. 核心包结构
5. 问题相关模块
6. 测试文件
7. 必要的配置文件

---

## 默认忽略目录和文件

除非我明确要求，否则不要读取：

- `.git/**`
- `.venv/**`
- `venv/**`
- `env/**`
- `ENV/**`
- `__pycache__/**`
- `.pytest_cache/**`
- `.mypy_cache/**`
- `.ruff_cache/**`
- `.tox/**`
- `.nox/**`
- `.coverage`
- `htmlcov/**`
- `build/**`
- `dist/**`
- `*.egg-info/**`
- `site-packages/**`
- `.vscode/**`
- `.idea/**`
- `.ipynb_checkpoints/**`
- `node_modules/**`
- `logs/**`
- `tmp/**`
- `temp/**`
- `data/**`
- `datasets/**`
- `assets/**`
- `static/**` 中的大型资源文件
- `media/**`
- `models/**`
- `checkpoints/**`
- `weights/**`
- `outputs/**`
- `runs/**`
- `wandb/**`
- `mlruns/**`

除非我明确要求，否则不要读取以下类型文件：

- 图片：`*.png`、`*.jpg`、`*.jpeg`、`*.gif`、`*.webp`、`*.bmp`
- 音频：`*.mp3`、`*.wav`、`*.flac`
- 视频：`*.mp4`、`*.avi`、`*.mov`
- 字体：`*.ttf`、`*.otf`、`*.woff`、`*.woff2`
- 压缩包：`*.zip`、`*.tar`、`*.gz`、`*.rar`、`*.7z`
- 模型权重：`*.pt`、`*.pth`、`*.onnx`、`*.ckpt`、`*.safetensors`、`*.h5`
- 大型数据：`*.csv`、`*.xlsx`、`*.parquet`、`*.jsonl`、`*.db`、`*.sqlite`、`*.pkl`
- 编译产物：`*.pyc`、`*.pyd`、`*.so`、`*.dll`、`*.dylib`

如果你认为必须读取上述目录或文件中的某个内容，请先说明：

1. 为什么必须读取
2. 需要读取哪个具体文件
3. 不读取会影响什么判断

然后等待我确认。

---

## 敏感信息规则

默认不要读取、复制、输出或总结以下文件内容：

- `.env`
- `.env.*`
- `*.pem`
- `*.key`
- `*.crt`
- `credentials.json`
- `secrets.json`
- `config.local.*`
- 含有 `token`、`secret`、`password`、`passwd`、`credential`、`apikey`、`api_key` 字样的文件

如果问题确实与配置有关，优先建议我提供脱敏后的配置片段。

---

## 问题定位规则

当我给出 bug、报错或功能问题时，请按以下流程处理：

1. 先阅读报错信息或问题描述。
2. 优先定位第一条真实错误，不要被后续连锁错误干扰。
3. 根据报错关键词、函数名、类名、模块名、文件路径搜索相关文件。
4. 不要读取整个项目。
5. 只读取和问题直接相关的文件。
6. 如果需要更多上下文，先列出候选文件和读取理由。
7. 给出原因分析和修改计划。
8. 等我确认后再修改代码。

---

## Python 报错处理规则

### ImportError / ModuleNotFoundError

优先检查：

- 报错模块名
- 当前运行目录
- 包结构和 `__init__.py`
- `pyproject.toml` / `setup.py` / `requirements.txt`
- `PYTHONPATH` 相关配置
- 是否使用 `src/` layout
- 是否缺少依赖安装
- 是否存在文件名与标准库或第三方库重名

不要一开始读取所有源码。

### SyntaxError / IndentationError

优先检查：

- 报错文件
- 报错行附近代码
- 缩进层级
- 括号、引号、冒号是否匹配
- Python 版本是否支持对应语法

### TypeError / AttributeError / ValueError

优先检查：

- 报错调用栈中的第一处项目代码
- 相关函数定义和调用位置
- 参数来源
- 返回值类型
- 可能为 `None` 的对象
- 数据结构字段是否存在

### RecursionError / RuntimeError / 死循环

优先检查：

- 递归终止条件
- 循环退出条件
- 迭代器或生成器使用
- 异步任务创建和等待逻辑

### FileNotFoundError / 路径问题

优先检查：

- 当前工作目录
- 相对路径和绝对路径
- 配置文件中的路径
- `Path(__file__).parent` 相关逻辑
- 资源文件是否应由用户提供

不要默认读取大型资源文件本身。

---

## 测试处理规则

当我要求修复测试失败或增加测试时，优先读取：

- 失败测试文件
- 被测试的模块
- `pytest.ini` / `pyproject.toml` 中的 pytest 配置
- `conftest.py`
- 相关 fixture

默认不要读取全部测试目录。

如果需要运行测试，先给出建议命令，例如：

```bash
pytest tests/test_xxx.py -q
```

除非我明确要求，否则不要直接执行测试命令。

---

## 修改规则

修改前必须先输出修改计划。

修改计划中必须包含：

1. 准备修改哪些文件
2. 每个文件为什么需要修改
3. 修改的具体思路
4. 可能的风险
5. 如何验证修改是否成功

没有我的确认，不要直接修改文件。

修改时应尽量：

- 保持改动最小化
- 不重构无关代码
- 不改变公共接口，除非我明确要求
- 不随意改动依赖版本
- 不随意格式化整个项目
- 不删除文件
- 不重命名项目结构
- 不改动大量文件来掩盖一个小问题

---

## 运行命令规则

除非我明确要求，否则不要直接执行：

- 安装依赖命令，例如 `pip install`、`poetry add`、`uv add`
- 删除命令，例如 `rm -rf`
- 数据库迁移命令
- 会修改远程服务或生产数据的命令
- 会长时间运行的训练、爬虫、服务启动命令
- 会产生大量输出的命令

可以先给出建议命令，等我确认后再执行。

如果必须执行命令，请优先选择只读、低风险命令，例如：

```bash
python --version
python -m pytest tests/test_xxx.py -q
python -m pip show package_name
```

---

## 依赖管理规则

不要随意新增依赖。

如果确实需要新增依赖，请说明：

1. 为什么标准库或现有依赖不够
2. 新依赖解决什么问题
3. 可能带来的兼容性和部署风险
4. 需要修改哪个依赖文件

优先根据项目现有工具选择命令：

- `requirements.txt`：使用 `pip`
- `pyproject.toml` + `poetry.lock`：使用 `poetry`
- `pyproject.toml` + `uv.lock`：使用 `uv`
- `Pipfile`：使用 `pipenv`
- `environment.yml`：使用 `conda`

---

## Web 项目规则

如果这是 Python Web 项目，优先识别框架：

- Flask
- FastAPI
- Django
- Tornado
- Starlette
- Sanic

优先分析：

- 路由定义
- 请求参数解析
- 数据模型
- 数据库访问层
- 配置加载
- 中间件
- 异常处理
- 启动入口

Django 项目优先查看：

- `manage.py`
- `settings.py`
- `urls.py`
- `views.py`
- `models.py`
- `serializers.py`
- `admin.py`
- `migrations/` 目录只在迁移问题相关时读取

FastAPI 项目优先查看：

- `main.py`
- `app.py`
- `routers/`
- `schemas/`
- `models/`
- `dependencies.py`
- 数据库 session 管理代码

默认不要读取数据库文件、上传文件或生产日志。

---

## 数据分析 / 机器学习项目规则

如果这是数据分析或机器学习项目，默认不要读取大型数据集、模型权重、训练输出。

优先读取：

- `README.md`
- 配置文件
- 数据加载代码
- 特征处理代码
- 模型定义代码
- 训练入口
- 推理入口
- 评估脚本
- 小型样例数据说明

Jupyter Notebook 处理规则：

- 优先读取代码单元和 Markdown 说明。
- 不要读取大型输出、图片、base64 内容。
- 如果 notebook 很大，先说明需要读取哪些部分。

---

## 爬虫 / 自动化项目规则

如果这是爬虫或自动化项目，优先分析：

- 入口脚本
- 请求逻辑
- 解析逻辑
- 限速和重试逻辑
- 登录和 cookie 处理
- 数据保存逻辑
- 配置文件

不要读取真实 cookie、token、账号密码。

不要直接运行可能访问外部网站、批量请求或修改远程数据的脚本，除非我明确要求。

---

## 异步 / 并发规则

如果项目使用 `asyncio`、线程池、进程池、Celery、RQ、APScheduler 等，优先检查：

- 任务创建位置
- `await` 是否缺失
- event loop 生命周期
- 共享资源访问
- 数据库连接管理
- 异常是否被吞掉
- 任务取消和超时逻辑

不要为了定位问题直接运行长时间任务。

---

## 代码风格规则

修改代码时遵循项目已有风格。

优先查看项目是否使用：

- `ruff`
- `black`
- `isort`
- `flake8`
- `mypy`
- `pyright`
- `pylint`

不要在没有必要时格式化整个项目。

如果只修改一个函数，就只调整相关区域。

---

## 输出风格

默认按以下结构回答：

1. 结论
2. 证据
3. 需要读取的文件
4. 修改计划或下一步建议

除非我要求详细解释，否则回答保持简洁。

如果我要求“只给结论”，不要展开长篇分析。

如果我正在学习 Python，请尽量用通俗语言解释模块、函数、类、对象、参数、返回值、异常、包结构等概念。

---

## 低 Token 约束

在每次任务中，请主动节省 token：

- 优先用目录结构判断项目。
- 优先用搜索定位文件。
- 优先读少量关键文件。
- 不重复读取已经分析过的内容。
- 如果已有 `PROJECT_SUMMARY.md`，先参考它。
- 如果已有 `FILE_INDEX.md`，先根据索引选择文件。
- 如果已有 `BUG_CONTEXT.md`，先围绕其中列出的文件分析。
- 读取源码时优先读取相关函数、类定义、报错行附近代码，不要一次性打开整个大文件。

---

## 推荐的工作方式

当我说“分析项目”时，你应该先回复类似：

> 我将先查看目录结构和构建/依赖配置，不会读取源码。然后我会列出建议读取的 3-5 个关键文件，等待你确认。

当我说“修复问题”时，你应该先回复类似：

> 我会先根据报错或问题描述定位相关文件，并给出修改计划，不会直接修改。若需要额外文件，我会先说明理由并等待确认。

当我说“解释代码”时，你应该先回复类似：

> 我会优先解释与当前问题直接相关的函数、类和调用链，不会展开整个项目。

---

## 禁止行为

除非我明确要求，否则不要：

- 一次性读取整个仓库
- 读取所有 `.py` 文件
- 读取虚拟环境或第三方依赖源码
- 读取 `.env`、密钥、token、证书等敏感文件
- 读取大型数据集、模型权重、日志、缓存、编译产物
- 大量输出无关解释
- 自动修改多个文件
- 自动执行危险命令
- 删除文件
- 重命名项目结构
- 随意新增依赖
- 随意修改依赖版本
- 随意格式化整个项目
- 改动运行方式但不说明风险

---

## 验证规则

修改后必须说明如何验证，例如：

- 运行哪条命令
- 运行哪个测试
- 启动哪个程序
- 检查哪个接口、页面、日志或输出
- 预期结果是什么

如果无法验证，请明确说明原因。
